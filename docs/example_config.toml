#
# AI
#
[ai.openai]
api_key = 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
model = "gpt-4o"

# [ai.anthropic]
# api_key = 'sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
# model = "claude-sonnet-4-20250514"

#
# Marketplace
#
[marketplace.facebook]
search_city = 'city'
# username and password can also be provided via FACEBOOK_USERNAME
# and FACEBOOK_PASSWORD environment variables.
username = 'username'
password = 'password'
login_wait_time = 60
search_interval = '30m'
max_search_interval = '1h'
seller_locations = ['city', 'surrounding city']
notify = 'user1'
exclude_sellers = []

#
# Users and Notifications
#
[user.user1]
pushbullet_token = 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
email = 'myself@gmail.com'
notify_with = ['gmail', 'pushover']

[user.user2]
email = 'user2@gmail.com'
notify_with = 'gmail'

[notification.pushover]
pushover_user_key = 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
pushover_api_token = 'someone@gmail.com'

[notification.gmail]
smtp_username = 'myself@gmail.com'
smtp_password = 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'

# Airtable: persist a structured row per listing for filtering/annotation.
# Requires `pip install ai-marketplace-monitor[airtable]`.
# [notification.airtable_listings]
# airtable_token = '${AIRTABLE_TOKEN}'           # Personal access token (pat...)
# airtable_base_id = 'appXXXXXXXXXXXXXX'         # Base ID from the Airtable URL
# airtable_table_name = 'Marketplace Listings'   # Optional, this is the default

#
# Items to search
#
[item.name1]
search_phrases = 'search word one'
search_region = 'usa'
search_interval = '1d'
delivery_method = 'shipping'
seller_locations = []

[item.name2]
search_phrases = ['search word one', 'search word two']
description = "it should be from manufacture, the seller should not offer shipping."
keywords = ['search word']
antikeywords = ['exclude word one', 'exclude word two']
notify = 'user2'
search_city = 'another city'
seller_locations = ['another city', 'surrounding city']
exclude_sellers = []
